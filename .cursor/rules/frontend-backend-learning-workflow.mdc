---
description: 写前端前先对齐后端；后端缺口暂停并给思路，不代写大批量 Python
alwaysApply: true
---

# 前后端协作：前端优先、后端自学

## 背景

- 开发者主要熟悉前端，Python/FastAPI 仍在学习中
- 本项目**不一味追求交付速度**，后端需要自己动手过一遍才清晰
- 一次性代写大量后端代码不利于学习（能看懂 ≠ 自己敲过）

## 触发场景

当用户描述「开始写某个前端页面 / 功能」时，Agent **先整体构思**，再决定能否动手写前端。

## 必须执行的流程

### 1. 先审后端，再写前端

开始前端实现前，快速检查是否已有支撑：

- API 端点（`app/api/v1/endpoints/`）
- Schema（`app/schemas/`）
- Service（`app/services/`）
- Model / 迁移（如需要）

### 2. 发现后端缺口 → 暂停前端

若缺少或明显有问题（字段不全、接口未定义、设计不合理等）：

- **停止**直接写前端页面 / 联调代码
- **不要**一次性代写完整后端实现
- 用结构化说明告诉用户「后端还缺什么」

### 3. 输出「后端待办 + 实现思路」（给用户自己敲）

每条缺口尽量包含：

| 项 | 内容 |
|----|------|
| 缺什么 | 例如：分组列表缺 `order_index`、`summary` 字段 |
| 改哪些文件 | model / schema / service / endpoint |
| 建议步骤 | 1→2→3 顺序 |
| 参考 | 项目中类似实现（如 `KnowledgeCommonPinService`） |
| 验收 | 接口返回示例 / 如何自测 |

优先**讲思路、指路径、给伪代码或片段**，完整代码由用户自己敲。

### 4. 用户确认后端完成后再写前端

用户说「后端好了 / 可以继续」之后，再：

- 补 `sk-types` / `sk-api`
- 写 Vue 页面与联调

## Agent 行为边界

**应该做：**

- 整体方案、前后端分工、API 契约设计
- 指出后端问题与改法
- 前端实现（在后端就绪后）
- 用户卡住时，小步答疑、Review 用户写的 Python

**不应该做（除非用户明确要求「帮我直接写后端」）：**

- 一次性落地大块后端功能
- 边写前端边悄悄补全套后端

## 输出模板（缺口时）

```markdown
## 前端方案（概要）
...

## 后端还缺（请先自行完善）
1. [ ] xxx
   - 文件：`app/services/xxx.py`
   - 思路：...
   - 参考：`KnowledgeCommonPinService.change_order_index`

## 后端完成后我再继续
- sk-types / sk-api / xxx.vue
```

## 例外

用户明确说「后端你也帮我写 / 直接实现」时，可代写，但仍建议**小步提交**（一个 API 或一个 Service 一块），并简短说明改了什么、为什么。
